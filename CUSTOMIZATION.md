# 🎨 Кастомизация SafeScan Bot сайта

Руководство по изменению текста, цветов и информации на сайте.

## 📝 Изменение текста

### Заголовок страницы
В файле `index.html` найдите строку:
```html
<title>SafeScan Bot - Защита от вирусов и вредоноса</title>
```
Измените на своё название.

### Логотип и название бота
```html
<div class="logo">
    <i class="fas fa-shield-alt"></i>
    <span>SafeScan</span>
</div>
```
Замените "SafeScan" на название вашего бота.

### Описание возможностей
Найдите секцию "Возможности" и измените:
- Заголовки: `<h3>Проверка ссылок</h3>`
- Описания: `<p>Мгновенно анализируем...</p>`
- Списки: измените пункты в `<ul class="feature-list">`

### Контактная информация
Найдите секцию "Контакты":
```html
<div class="contact-item">
    <i class="fas fa-envelope"></i>
    <h3>Email</h3>
    <p>support@safescan.bot</p>  <!-- Измените здесь -->
</div>
```

Измените:
- Email
- Телефон
- Telegram
- Веб-сайт

---

## 🎨 Изменение цветов

Все цвета определены в `styles.css` в переменных CSS (`:root`):

```css
:root {
    --primary-color: #6366f1;      /* Основной цвет (индиго) */
    --secondary-color: #ec4899;     /* Вторичный цвет (розовый) */
    --accent-color: #06b6d4;        /* Акцент (циан) */
    --dark-bg: #0f172a;             /* Темный фон */
    --light-bg: #f8fafc;            /* Светлый фон */
    --text-dark: #1e293b;           /* Темный текст */
    --text-light: #64748b;          /* Светлый текст */
    --border-color: #e2e8f0;        /* Границы */
}
```

### Примеры изменения:

**Изменить основной цвет с индиго на синий:**
```css
--primary-color: #0066cc;
```

**Изменить вторичный цвет на оранжевый:**
```css
--secondary-color: #ff6b35;
```

**Изменить акцент на зелёный:**
```css
--accent-color: #00b894;
```

### Популярные цвета (Hex коды):

**Синие оттенки:**
- #0066cc - Синий
- #0099ff - Светлый синий
- #003399 - Темно-синий

**Красные оттенки:**
- #ff0000 - Красный
- #cc0000 - Темно-красный
- #ff3333 - Светло-красный

**Зелёные оттенки:**
- #00b894 - Зелёный
- #27ae60 - Тёмный зелёный
- #00ff00 - Лайм

**Оранжевые оттенки:**
- #ff6b35 - Оранжевый
- #ff9500 - Жёлто-оранжевый
- #ff7f00 - Тёмно-оранжевый

**Фиолетовые оттенки:**
- #9b59b6 - Фиолетовый
- #8e44ad - Темно-фиолетовый
- #a569bd - Светлый фиолетовый

---

## 🌈 Изменение градиентов

Градиенты используются в нескольких местах. Найдите их в `styles.css`:

```css
/* Градиент в героя-секции */
.hero {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #4facfe 75%, #00f2fe 100%);
}

/* Градиент в кнопках */
.btn-primary {
    background: linear-gradient(135deg, #ec4899 0%, #f97316 100%);
}
```

### Синтаксис градиента:
```css
background: linear-gradient(направление, цвет1 позиция%, цвет2 позиция%, ...);
```

### Примеры направлений:
- `to right` - слева направо
- `to bottom` - сверху вниз
- `135deg` - диагональ
- `90deg` - вправо
- `180deg` - вниз

### Простой двухцветный градиент:
```css
background: linear-gradient(to right, #ff0000, #0000ff);
```

---

## 📐 Изменение размеров

### Размер шрифта
```css
.hero h1 {
    font-size: 3.5rem;  /* Измените число */
}

.features h2 {
    font-size: 2.5rem;  /* Больше = больше */
}
```

### Размер кнопок
```css
.btn {
    padding: 12px 30px;  /* Высота и ширина */
}

.btn-large {
    padding: 15px 40px;  /* Больше = больше кнопка */
}
```

### Размер иконок
```css
.shield-animation {
    font-size: 10rem;  /* Измените число */
}

.icon-box {
    font-size: 2rem;  /* Размер иконок в карточках */
}
```

---

## 🔤 Изменение шрифта

В начале `styles.css`:
```css
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
```

Замените на другой шрифт:

**Другие безопасные шрифты:**
- `Arial, sans-serif`
- `Times New Roman, serif`
- `Georgia, serif`
- `Courier New, monospace`
- `Comic Sans MS, cursive`

**Подключение Google Fonts:**

Добавьте в `<head>` в `index.html`:
```html
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700;800&display=swap" rel="stylesheet">
```

Затем в `styles.css`:
```css
body {
    font-family: 'Poppins', sans-serif;
}
```

---

## 📱 Изменение адаптивности

Если хотите изменить точку разлома для мобильных, найдите в `styles.css`:

```css
@media (max-width: 768px) {
    /* Стили для планшетов и меньше */
}

@media (max-width: 480px) {
    /* Стили для мобильных */
}
```

Измените `768px` на другое значение:
- `1024px` - для более широких экранов
- `600px` - для узких экранов

---

## 🔄 Изменение анимаций

### Скорость анимаций
Найдите `animation:` в `styles.css`:

```css
animation: gradient 15s ease infinite;  /* 15s = скорость */
```

Измените `15s` на:
- `5s` - быстро
- `10s` - нормально
- `20s` - медленно

### Эффект параллакса
```css
hero.style.backgroundPosition = `0% ${window.pageYOffset * 0.5}px`;
                                              /* 0.5 = интенсивность */
```

Измените `0.5` на:
- `0.3` - слабый эффект
- `0.7` - сильный эффект

---

## 🖼️ Добавление своей иконки

Все иконки из Font Awesome. Чтобы найти нужную:
1. Перейдите на https://fontawesome.com/icons
2. Поищите иконку
3. Скопируйте код: `<i class="fas fa-название"></i>`

Примеры:
```html
<!-- Щит -->
<i class="fas fa-shield-alt"></i>

<!-- Сердце -->
<i class="fas fa-heart"></i>

<!-- Звёзда -->
<i class="fas fa-star"></i>

<!-- Замок -->
<i class="fas fa-lock"></i>

<!-- Компьютер -->
<i class="fas fa-computer"></i>
```

---

## 🎬 Изменение количества секций

### Добавить новую секцию:
```html
<section id="new-section" class="new-section">
    <div class="container">
        <h2>Новая секция</h2>
        <p>Ваше содержимое здесь</p>
    </div>
</section>
```

Добавьте стили в `styles.css`:
```css
.new-section {
    padding: 100px 0;
    background: white;
}
```

Добавьте ссылку в навигацию `index.html`:
```html
<li><a href="#new-section">Новая секция</a></li>
```

---

## 📊 Изменение статистики

Найдите в `index.html`:
```html
<div class="stat-item">
    <h3>1M+</h3>
    <p>Пользователей</p>
</div>
```

Измените числа и описания на свои.

---

## 🎨 Тёмная тема

Добавьте в `styles.css` в конце:
```css
@media (prefers-color-scheme: dark) {
    body {
        background-color: #1e293b;
        color: #f1f5f9;
    }
    
    .feature-card {
        background: #334155;
    }
}
```

---

## 🌍 Изменение языка

Просто замените все текстовые строки в `index.html` и `script.js` на нужный язык. Все сообщения находятся в HTML или в JavaScript комментариях.

---

## 💾 Сохранение и проверка

После изменений:
1. Сохраните файл (Ctrl+S)
2. Перезагрузите браузер (F5 или Ctrl+Shift+R)
3. Очистите кеш если нужно
4. Проверьте на всех устройствах

---

## 🎯 Быстрые идеи кастомизации

1. **Изменить цветовую схему**: Измените 3 переменные в `:root`
2. **Добавить логотип**: Замените иконку в `<div class="logo">`
3. **Изменить контакты**: Обновите информацию в секции "Контакты"
4. **Ускорить анимации**: Уменьшите время в `animation:`
5. **Увеличить шрифт**: Увеличьте `font-size` везде

---

## ❓ Если что-то сломалось

1. **Проверьте синтаксис** - закрывающие теги, кавычки
2. **Очистите кеш** - Ctrl+Shift+Del
3. **Откройте консоль** - F12, вкладка Console
4. **Восстановите из Git** - `git checkout styles.css`

---

**Удачи с кастомизацией!** 🎨✨
